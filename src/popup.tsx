import React from 'react';
import ReactDOM from 'react-dom';

const Popup = () => {
  return null;
};

ReactDOM.render(
  <React.StrictMode>
    <Popup />
  </React.StrictMode>,
  document.getElementById('root')
);
